# Test dependencies for the Claude Agents Dashboard.
#
# Installed on top of requirements.txt by run-tests.sh / run-coverage.sh /
# run-e2e-coverage.sh (and `make setup`). Running the dashboard itself does
# not need any of these, which is why they live in their own file.
#
# Keep this list to what the suite actually imports or loads — an entry no
# test uses is an entry that rots.

pytest>=8.0.0
pytest-asyncio>=0.23.0   # asyncio_mode=auto (pytest.ini)
pytest-cov>=4.0.0        # --cov in run-coverage.sh / run-e2e-coverage.sh
httpx>=0.24.0            # AsyncClient + ASGITransport in the route tests
